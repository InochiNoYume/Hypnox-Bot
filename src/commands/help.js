const { SlashCommandBuilder, EmbedBuilder } = require('discord.js'); const {canViewHelpCategory}=require('../utils/helpPermissions');
const C={informacion:['/info','/reglas','/roles','/links','/user','/serverinfo','/credits'],moderacion:['/moderacion warn','/moderacion warnings','/moderacion unwarn','/moderacion timeout','/moderacion clear','/moderacion slowmode','/moderacion kick','/moderacion ban'],tickets:['/tickets panel','/tickets cerrar','/tickets claim','/tickets add','/tickets remove'],anuncios:['/anuncio'],eventos:['/evento crear','/evento editar','/evento cancelar','/evento iniciar','/evento finalizar'],comunidad:['/user','/serverinfo','/credits'],premios:['/premio crear','/premio finalizar','/premio reroll','/premio entregar'],administracion:['/administracion config','/administracion set-channel','/administracion set-role','/administracion set-permission','/administracion maintenance','/administracion reload']};
const data=new SlashCommandBuilder().setName('help').setDescription('Muestra la ayuda por categoría').addSubcommand(s=>s.setName('informacion').setDescription('Información')).addSubcommand(s=>s.setName('moderacion').setDescription('Moderación')).addSubcommand(s=>s.setName('tickets').setDescription('Tickets')).addSubcommand(s=>s.setName('anuncios').setDescription('Anuncios')).addSubcommand(s=>s.setName('eventos').setDescription('Eventos')).addSubcommand(s=>s.setName('comunidad').setDescription('Comunidad')).addSubcommand(s=>s.setName('premios').setDescription('Premios')).addSubcommand(s=>s.setName('administracion').setDescription('Administración'));
async function execute(i){const cat=i.options.getSubcommand();if(!canViewHelpCategory(i.member,cat))return i.reply({content:'No tienes permisos para consultar esta categoría.',ephemeral:true});return i.reply({embeds:[new EmbedBuilder().setColor(0).setTitle(`HYPNOX STUDIOS — ${cat.toUpperCase()}`).setDescription(C[cat].map(x=>`\`${x}\``).join('\n')).setTimestamp()],ephemeral:true});}
module.exports={data,execute};
