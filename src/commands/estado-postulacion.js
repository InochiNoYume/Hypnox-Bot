const { SlashCommandBuilder, MessageFlags } = require('discord.js');
const { getGuildType } = require('../utils/guild'); const { isAdminOrHigher, getConfiguredChannel, accessDenied, missingChannel, base } = require('../utils/postulacionesPanels');
const EPHEMERAL=MessageFlags.Ephemeral;
const data=new SlashCommandBuilder().setName('estado-postulacion').setDescription('Publica el estado actual de la convocatoria.').addStringOption(o=>o.setName('estado').setDescription('Estado actual de la convocatoria.').setRequired(true).addChoices({name:'Abierta',value:'ABIERTA'},{name:'Cerrada',value:'CERRADA'},{name:'En revisión',value:'EN REVISIÓN'},{name:'Resultados publicados',value:'RESULTADOS PUBLICADOS'})).addStringOption(o=>o.setName('detalle').setDescription('Información adicional opcional.').setRequired(false));
async function execute(interaction){
 if(getGuildType(interaction.guildId)!=='applications')return interaction.reply({content:'Este comando solo está disponible en el Discord Staff Applications.',flags:EPHEMERAL});if(!isAdminOrHigher(interaction.member))return interaction.reply({content:accessDenied(),flags:EPHEMERAL});
 await interaction.deferReply({flags:EPHEMERAL});
 try{const {channel,envKey}=await getConfiguredChannel(interaction,'estado');if(!channel)return interaction.editReply({content:missingChannel(envKey||'APPLICATIONS_CHANNEL_STATUS_ID')});const status=interaction.options.getString('estado',true);const detail=interaction.options.getString('detalle');const embed=base('ESTADO DE POSTULACIÓN',`Estado actual de la convocatoria: **${status}**.`);embed.addFields({name:'◆ ESTADO ACTUAL',value:status},{name:'◆ INFORMACIÓN',value:detail||'Sin información adicional.'},{name:'◆ FUENTE OFICIAL',value:'Este mensaje corresponde al estado publicado oficialmente por Hypnox Studios. Revisa este canal para futuras actualizaciones.'});embed.setFooter({text:'Hypnox Studios • Estado de Postulación'});await channel.send({embeds:[embed]});return interaction.editReply({content:`Estado publicado en <#${channel.id}>.`});}catch(error){console.error('[HYPNOX] Application status error:',error);return interaction.editReply({content:'No se pudo publicar el estado de la postulación.'}).catch(()=>{});}
}
module.exports={data,execute,guilds:['applications']};
