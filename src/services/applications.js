const supabase = require('../database/supabase');
const { ensureUser } = require('./moderation');
const { getGuildRow } = require('./guilds');
async function activeApplication(guildId, userId){const guild=await getGuildRow(guildId);if(!guild)return null;const user=await ensureUser({id:userId,username:'discord-user',globalName:'discord-user'});const {data,error}=await supabase.from('applications').select('*').eq('guild_id',guild.id).eq('applicant_user_id',user.id).order('submitted_at',{ascending:false}).limit(1).maybeSingle();if(error)throw error;return data;}
async function createApplication(guildId,userId,formResponse={}){const guild=await getGuildRow(guildId);const user=await ensureUser({id:userId,username:'discord-user',globalName:'discord-user'});if(!guild)throw new Error('Guild no registrada');const existing=await activeApplication(guildId,userId);if(existing&&existing.cooldown_until&&new Date(existing.cooldown_until)>new Date())return {cooldown:existing.cooldown_until};const {data,error}=await supabase.from('applications').insert({guild_id:guild.id,applicant_user_id:user.id,phase:1,status:'submitted',form_response:formResponse,submitted_at:new Date().toISOString()}).select().single();if(error)throw error;return data;}
async function updateApplication(id,patch){const {data,error}=await supabase.from('applications').update(patch).eq('id',id).select().single();if(error)throw error;return data;}
async function createInterview(applicationId,interviewerId,scheduledAt){const {data,error}=await supabase.from('interviews').insert({application_id:applicationId,interviewer_discord_user_id:interviewerId,scheduled_at:scheduledAt,status:'scheduled'}).select().single();if(error)throw error;return data;}
module.exports={activeApplication,createApplication,updateApplication,createInterview};
